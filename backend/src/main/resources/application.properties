spring.application.name=backend


# === MySQL Database Connection ===
spring.datasource.url=jdbc:mysql://localhost:3306/instagram
spring.datasource.username=root
spring.datasource.password=1234567890

# ✅ Use environment port (required by Render)
server.port=${PORT:8080}

# === JPA Settings ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Limit Hibernate to use fewer connections
spring.datasource.hikari.maximum-pool-size=2

# JDBC Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# === Spring Security (optional login)
spring.security.user.name=saran
spring.security.user.password=saran123

